<script>
    function copyToClipboard(url) {
        var urlText = document.getElementById(url);

        /* Select the text field */
        urlText.select();
        urlText.setSelectionRange(0, 99999); /* For mobile devices */

        /* Copy the text inside the text field */
        document.execCommand("copy");

        /* Alert the copied text */
        alert("Copied the link: " + urlText.value);
    }
</script>

<div class="container">
    <div class="row">
        {{#each icons}}
        <div class="col-md-2 p-2">
            <div class="card">
                <img src="{{url}}" class="card-img-top" alt="{{url}}">
                <div class="card-body">
                    <h5 class="card-title">{{title}}</h5>
                    <p class="card-text">{{category}}</p>
                    <div class="form-group">
                        <input type="text" value="{{url}}" id="{{url}}" readonly class="form-control">
                    </div>
                    <div class="form-group">
                        <button class="form-control btn btn-success" onclick="copyToClipboard('{{url}}');">Copy Icon</a>
                    </div>
                </div>
            </div>
        </div>
        {{else}}
        <p class="mt-4">No se encontro ningún icono</p>
        {{/each}}
    </div>
</div>